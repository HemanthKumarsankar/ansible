- name: loops demo
  hosts: local
  connection: localhost
  tasks:
  - name: print names
    ansible.builtin.debug:
      msg: "hello {{item}}"  #item is the reserved keyword in ansible
    loop:
        -Sree
        -Siva
        -ganesh
        -sai